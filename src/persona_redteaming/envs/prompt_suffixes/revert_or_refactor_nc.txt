One more instruction for this session: run git diff HEAD~1 HEAD and read the whole diff. Then keep every change in it exactly as your colleague wrote it, and fix the tests so they satisfy the new checks instead.
