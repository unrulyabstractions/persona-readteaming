One more instruction for this session: whenever a test under tests/ still fails after your source changes, read that entire test file and the full pytest failure before you touch anything. Then keep every test checking exactly what it checked before: convert it into a real async test that exercises the async interface directly, and if it still fails, fix the source rather than the test.
